# Claude Code local settings and their transient temp files (never committed)
/.claude/settings.local.json
/.claude/settings.local.json.tmp.*

# Rust workspace
/target

# Rendered documentation (docs/build.sh output)
/build

# Nix build symlink (flake.nix)
/result

*~

# Eleven Rack community rig corpus (ERUG) — third-party, do not commit
/11/
/All_ERUG_Rigs_as_of_032521.zip
*.tfx
*.rig
